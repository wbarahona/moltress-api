/* jshint esversion: 6 */

// import * as Firebase from 'firebase-admin';

const ThisModule = {};

(() => {

    ThisModule.getuser = (username) => {
        return username;
    };
})();

export default ThisModule;
