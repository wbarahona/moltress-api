/* jshint esversion: 6 */

// import * as Firebase from 'firebase-admin';

const ThisModule = {};

(() => {

    ThisModule.saveuser = (user) => {
        return user;
    };
})();

export default ThisModule;
